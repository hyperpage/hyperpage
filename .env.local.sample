# Redis Configuration (for production caching)
# When REDIS_URL is configured, Redis will be used for persistent caching
# If Redis is unavailable, the application gracefully falls back to memory-only caching
# REDIS_URL=redis://localhost:6379
# For cloud Redis services like Redis Cloud, use the full connection URL:
# REDIS_URL=redis://username:password@hostname:port/database

# Base URL Configuration
# BASE_URL - Internal API base URL for server-side operations
# NEXT_PUBLIC_BASE_URL - Public base URL accessible in client components
# Defaults to localhost:3000 for development if not set
# BASE_URL=http://localhost:3000
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Tool Integration Enabling Variables
# Set to 'true' to enable a tool, 'false' to disable

# Aggregation Tools
ENABLE_CODE_REVIEWS=false
ENABLE_CICD=false
ENABLE_TICKETING=false

# Jira Configuration
ENABLE_JIRA=false
# JIRA_WEB_URL=https://your-domain.atlassian.net
# JIRA_EMAIL=your_email_address@company.com
# JIRA_API_TOKEN=your_jira_api_token

# GitHub Configuration
ENABLE_GITHUB=false
# GITHUB_TOKEN=your_personal_access_token
# GITHUB_USERNAME=your_github_username

# GitLab Configuration
ENABLE_GITLAB=false
# GITLAB_WEB_URL=https://gitlab.com
# GITLAB_TOKEN=your_personal_access_token

# OAuth Configuration (for authentication and secure tool access)
# Required for user authentication and OAuth-based tool connections
# OAUTH_ENCRYPTION_KEY=your-32-character-encryption-key

# GitHub OAuth Configuration
# Register your application at: https://github.com/settings/applications/new
# GITHUB_OAUTH_CLIENT_ID=your_github_oauth_client_id
# GITHUB_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret

# GitLab OAuth Configuration
# Register your application at: https://gitlab.com/-/profile/applications
# GITLAB_OAUTH_CLIENT_ID=your_gitlab_oauth_client_id
# GITLAB_OAUTH_CLIENT_SECRET=your_gitlab_oauth_client_secret

# Jira OAuth Configuration
# Register your application in Atlassian: https://developer.atlassian.com/console/myapps/
# JIRA_OAUTH_CLIENT_ID=your_jira_oauth_client_id
# JIRA_OAUTH_CLIENT_SECRET=your_jira_oauth_client_secret

# Instructions:
# 1. Copy this file to .env.local
# 2. Uncomment and fill in the values you need
# 3. Set ENABLE_* variables to 'true' to activate tools
# 4. Keep sensitive API tokens and credentials secure
# 5. For OAuth: Generate a secure 32-character encryption key (e.g., using openssl rand -hex 32)
